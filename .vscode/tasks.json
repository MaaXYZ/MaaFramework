{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_picli",
            "type": "shell",
            "command": "cmake --build build --preset 'NinjaMulti - Debug' -j 16"
        }
    ]
}