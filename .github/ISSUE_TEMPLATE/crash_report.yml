name: 崩溃报告 / Crash Report
description: 程序崩溃问题 / Program crash issue
labels: ["bug", "crash"]
body:
    - type: markdown
      attributes:
          value: |
              ## 提交前请阅读 / Please Read Before Submitting

              📖 **[问题反馈指南 / Troubleshooting Guide](https://github.com/MaaXYZ/MaaFramework/blob/main/docs/zh_cn/5.1-问题反馈.md)**

              ⚠️ **请先更新到最新版本的 MaaFramework，确认问题是否仍然存在后再提交 Issue。**
              ⚠️ **Please update to the latest version of MaaFramework and verify the issue still exists before submitting.**

              崩溃问题需要提供 **精确的版本号** 和 **Crash Dump 文件**，否则无法进行分析。
              Crash issues require **exact version** and **Crash Dump file** for analysis.

    - type: textarea
      id: description
      attributes:
          label: 崩溃描述 / Crash Description
          description: 请描述崩溃前的操作 / Describe what you were doing before the crash
      validations:
          required: true

    - type: input
      id: version
      attributes:
          label: MaaFramework 版本 / Version
          description: ⚠️ 必须提供精确版本号，崩溃分析依赖对应版本的调试符号 / Exact version required for debug symbols
          placeholder: "例如 / e.g.: v2.0.0"
      validations:
          required: true

    - type: textarea
      id: environment
      attributes:
          label: 运行环境 / Environment
          description: 操作系统、设备信息等 / OS, device info, etc.
          placeholder: |
              OS: Windows 11 / macOS 14 / Ubuntu 22.04
              Device: ...
      validations:
          required: true

    - type: textarea
      id: reproduce
      attributes:
          label: 复现步骤 / Steps to Reproduce
          description: 崩溃前的操作步骤 / Steps taken before the crash
          placeholder: |
              1. ...
              2. ...
              3. ...
      validations:
          required: true

    - type: textarea
      id: dump
      attributes:
          label: Crash Dump 文件 / Crash Dump File
          description: |
              **Windows**: 通常位于 `C:\Users\<用户名>\AppData\Local\CrashDumps\`，文件格式为 `.dmp`
              **Linux/macOS**: Core dump 文件或系统日志

              请确认文件修改日期与崩溃时间对应。
              Verify the file modification date matches the crash time.
          placeholder: 拖拽上传 .dmp 文件 / Drag and drop .dmp file here
      validations:
          required: true

    - type: textarea
      id: logs
      attributes:
          label: 日志文件 / Log Files
          description: |
              请上传 maa.log 文件（位于 `<工作目录>/debug/maa.log`）
              Please upload maa.log file (located at `<working_dir>/debug/maa.log`)
          placeholder: 拖拽上传 maa.log 文件 / Drag and drop maa.log file here
      validations:
          required: true

    - type: textarea
      id: additional
      attributes:
          label: 其他信息 / Additional Information
          description: 其他可能有帮助的信息 / Any other helpful information
      validations:
          required: false
